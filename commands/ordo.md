---
description: Activate ORDO — auto-apply lossless compaction + the classify→route dispatcher + the gates for this session.
---

Activate **ORDO** for this session and keep it active from now on (it does not need re-invoking).

1. **Load the profile.** Read `.claude/skills/ordo/references/OPERATING-PROFILE.md` if it exists; else read
   `OPERATING-PROFILE.md` at the repo root; else read `.claude/skills/ordo-lean/SKILL.md` (Lean tier). Follow it as
   standing instructions.
2. **Classify, then route.** Triage this task light vs strict (5 hard signals: reversibility · real-fork · horizon ·
   breadth · load-bearing facts). LIGHT → answer directly + compress. STRICT → arm the ledger + the right gate.
3. **Compress everything.** Output: format-by-shape (TSV/minified, never pretty-print) + ponytail. Inbound + every
   tool result (crawl/transcript/PDF): route through the lossless inbound compactor (measured-revert, never inflate).
4. **Persist (strict tasks).** If `.ordo/ledger.md` exists, read it at the start and append decisions as you work.

Confirm in one line that ORDO is active, then continue with the request below.

$ARGUMENTS
